export enum Theme {
  Dark = 'dark',
  Light = 'light',
}

export enum Direction {
  Up = 'up',
  Down = 'down',
  Left = 'left',
  Right = 'right',
}
export enum LocalStorageKey {
  playerTrackableId = 'playerTrackableId',
  theme = 'theme',
}
